<div class="container-fluid">
    <div class="row flex-nowrap">
        {{> sidebar}}
        <div class="col py-3">
            <div class="bg-purple">
                <h1>{{subject}}</h1>
            </div>
            {{> uploader}}
            <div id="collection-content">
                {{#each collection}}
                    {{> loadedfile}}
                {{/each}}
            </div>
        </div>
    </div>
</div>